<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="jobplatform.mapper.AccountMapper">

  <select id="findByEmail" parameterType="string" resultType="jobplatform.model.Account">
    SELECT id, email, password, username, role, ref_id as refId
    FROM account
    WHERE email = #{email}
    LIMIT 1
  </select>

  <insert id="insertAccount" parameterType="jobplatform.model.Account" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO account(email, password, username, role, ref_id)
    VALUES (#{email}, #{password}, #{username}, #{role}, #{refId})
  </insert>

</mapper>
